query GetAllPaymentMethod($shopBranchId: ID) {
  getAllPaymentMethod(shopBranchId: $shopBranchId) {
    label
    value
    balanceAvailable
  }
}

mutation generateMacZaloMiniApp($dataMac: Mixed) {
  generateMacZaloMiniApp(dataMac: $dataMac)
}
