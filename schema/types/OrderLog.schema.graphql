type OrderLog {
  id: String
  createdAt: DateTime
  updatedAt: DateTime
  orderId: ID
  type: String
  memberId: ID
  toMemberId: ID
  customerId: ID
  note: String
  statusText: String
  order: Order
  member: Member
  toMember: Member
  customer: Customer
}
